# Do Not Edit This Repository Directly

This is a published mirror of `plugin/` from the private CatPlan repo.
All changes are wiped and replaced on each `npm run publish-plugin` from CatPlan.
File issues / open PRs against the upstream CatPlan repo instead.
